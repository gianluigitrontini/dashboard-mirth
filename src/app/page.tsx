import { redirect } from "next/navigation";

export default async function App() {
  // const loggedIn = true;
  // if (loggedIn) {
  //   redirect("/dashboard");
  // } else {
  //   redirect("/auth/login");
  // }
}
